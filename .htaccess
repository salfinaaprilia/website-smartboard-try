RewriteEngine On

# Serve the matching .html file for a clean URL (no visible redirect, URL stays as-is)
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.html -f
RewriteRule ^([^.]+)$ $1.html [NC,L]
